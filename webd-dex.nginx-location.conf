# Adaugă acest bloc în configurația nginx existentă pentru webdollar.cloudns.nz
# (în interiorul blocului server {} existent)

    location /dex/ {
        alias /var/www/webd-dex/;
        index index.html;
        try_files $uri $uri/ /dex/index.html;
    }
